# Project environment variables
export PROJECT_NAME="scheme-primer"
export PROJECT_ROOT="$(pwd)"

# Guile configuration
export GUILE_LOAD_PATH="${PROJECT_ROOT}:${GUILE_LOAD_PATH}"

# Development environment
export EDITOR="emacs -nw -Q -l ${PROJECT_ROOT}/scheme-primer.el"

# Display project info
echo "Scheme Primer Development Environment"
echo "Project: ${PROJECT_NAME}"
echo "Root: ${PROJECT_ROOT}"
echo ""
echo "Run 'make dev' to start tmux+Emacs environment"
echo "Run 'make help' for available commands"